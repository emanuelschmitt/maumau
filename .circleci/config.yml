version: 2.1
jobs:
  build_and_test:
    docker:
      - image: circleci/node:12.18.2
    steps:
      - checkout
      - run: yarn install
      - run: yarn lint
      - run: yarn build
      - run: yarn test
  deploy:
    machine: true
    environment:
      HEROKU_APP: 'maumau-io' # define env var $HEROKU_APP
    steps:
      - checkout
      - heroku/check-authentication
      - docker build .
      - heroku/push-docker-image
      - heroku/release-docker-image
workflows:
  version: 2
  build_test_deploy:
    jobs:
      - build_and_test
      - deploy
orbs:
  heroku: circleci/heroku@1.2.2
